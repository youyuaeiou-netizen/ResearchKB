# ResearchKB 顶层规则

适用范围：本仓库根目录及其子目录（当前检出位置：`F:\ResearchKB`）。通用协作规则以本机工作台规则为权威来源，并视为本文件已引用的内容；本文件仅补充 ResearchKB 专属规则。

## Scope, Authorization, and Safety

- 默认只读；仅在用户明确授权时作最小、原位且可验证的修改。保留人工内容、原始数据、实验观察、既有结构和无关文件。
- 删除、覆盖、批量迁移、数据库/原始数据/凭据、外部写入与不可逆操作，先说明影响并取得明确授权；不得暴露秘密。
- 高风险变更先检查目标并使用 dry-run、精确 diff 或可回滚方式；完成后仅以实际验证结果声明成功。

## ResearchKB Workspace

- 仓库根目录是整个 ResearchKB 的 Codex 工作区根目录（当前为 `F:\ResearchKB`）；`.harness` 是自动化执行层，不是独立工作区。
- 临时文件、日志、缓存、运行结果、脚本、配置、测试与中间产物统一存入 `.harness`，不得污染知识库根目录。
- 正式知识仅在 `00-Ideas`、`01-Projects`、`02-Areas`、`03-Resources`、`04-Archive`、`05-Skills` 中按授权修改；`.obsidian`、`_system`、`.claudian` 默认只读。
- 自动化脚本从自身位置解析路径，不得依赖 `.harness` 或调用时 cwd；应能以任意本地检出目录为工作区根正常运行。

## 知识与自动化边界

- Zotero 是书目、PDF 与引用的权威源；仅通过 Local API 只读访问。自动候选仅可写入 `00-Ideas\v3-auto` 或 `03-Resources\v3-auto`，保持待人工审阅状态。
- 自动更新只改 `CODEX MANAGED` 区域，不覆盖、移动或删除既有知识；来源失效、冲突或重复时只生成标记、报告或提案。
- 正式 Vault 写入默认 dry-run，只有用户明确授权 `--apply` 后才执行；具体执行、来源与调度规则见 `.harness\AGENTS.md`。
- 处理材料知识时完整读取指定原文与必要上下文，保留定义、条件、公式及单位、机制、适用范围、例外与结构—组织—工艺—性能关系；不录入习题或完整解题过程。
